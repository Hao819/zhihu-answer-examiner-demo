# 服务端监听端口；部署平台通常会自动注入 PORT（CloudBase HTTP 云函数固定 9000）。
PORT=9000

# 监听地址；部署环境需保持 0.0.0.0。
HOST=0.0.0.0

# 知乎开放平台 Access Secret。
# 本地放在未提交的 .env 中；生产在 CloudBase 控制台「云函数 → 函数配置 → 环境变量」配置。
ZHIHU_ACCESS_SECRET=

# 注：直答模型档位已固化在 server.js 中，不再通过环境变量配置。
